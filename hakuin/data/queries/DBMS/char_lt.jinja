SELECT  {{ ctx.column | sql_escape | sql_len }} != {{ ctx.s | length }}
        AND
        {{ ctx.column | sql_escape | sql_char_at(ctx.s | length) | sql_unicode }} < {{ n }}
FROM    {{ ctx.table | sql_escape }}
LIMIT   1
OFFSET  {{ ctx.row_idx }}