SELECT  {{ 'lower(DATA_TYPE)' | sql_in_str_set(types) }}
FROM    information_schema.columns
WHERE   TABLE_SCHEMA=database() AND
        TABLE_NAME={{ ctx.table | sql_hex_lit }} AND
        COLUMN_NAME={{ ctx.column | sql_hex_lit }}